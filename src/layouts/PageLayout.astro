---
import { Image } from 'astro:assets';
import BaseLayout from './BaseLayout.astro';
import LanguageSwitcher from '../components/seo/LanguageSwitcher.astro';
import Button from '../components/ui/Button.astro';
import NewsletterForm from '../components/forms/NewsletterForm.astro';
import logo from '../assets/brand/optima-mind-logo.png';
import { getNav, getRdvHref } from '../data/nav';
import { getSettings } from '../lib/content';
import { t, type Locale } from '../lib/i18n';
import { withLocale } from '../lib/locale';
import { SITE_FALLBACK, telHref } from '../lib/site';

interface Props {
  lang: Locale;
  title: string;
  description: string;
  path: string;
  noindex?: boolean;
  jsonLd?: Record<string, unknown> | Record<string, unknown>[];
  ogImage?: string;
  /** true quand la page est servie en fallback (contenu FR sous une URL /pl/). */
  usedFallback?: boolean;
  /** URL canonique explicite (fallback → version FR). */
  canonicalHref?: string;
}

const {
  lang,
  title,
  description,
  path,
  noindex,
  jsonLd,
  ogImage,
  usedFallback = false,
  canonicalHref,
} = Astro.props;

const nav = getNav(lang);
const rdvHref = getRdvHref(lang);
const current = withLocale(lang, path);
const year = new Date().getFullYear();
// Réglages globaux depuis le CMS (fallback : valeurs réelles en dur).
const settings = (await getSettings())?.data ?? SITE_FALLBACK;
const address = settings.address;
const siteName = settings.name;
---

<BaseLayout
  lang={lang}
  title={title}
  description={description}
  path={path}
  noindex={noindex}
  jsonLd={jsonLd}
  ogImage={ogImage}
  canonicalHref={canonicalHref}
>
  <a class="skip-link" href="#main">{t(lang, 'skip-link')}</a>

  <header class="site-header">
    <div class="container site-header__inner">
      <a
        class="site-header__brand"
        href={`/${lang}/`}
        aria-label={`${siteName} — ${t(lang, 'nav.home')}`}
      >
        <Image class="site-header__mark" src={logo} alt="" width={80} height={80} loading="eager" />
        <span class="site-header__wordmark">{siteName}</span>
      </a>

      <nav id="site-nav" class="site-header__nav" aria-label={t(lang, 'footer.nav')}>
        <ul class="site-header__nav-list">
          {
            nav.map((item) => (
              <li>
                <a
                  class="site-header__nav-link"
                  href={item.href}
                  aria-current={item.href === current ? 'page' : undefined}
                >
                  {item.label}
                </a>
              </li>
            ))
          }
        </ul>
      </nav>

      <LanguageSwitcher lang={lang} path={path} />

      <button
        class="site-header__toggle"
        type="button"
        aria-expanded="false"
        aria-controls="site-nav"
        aria-label={t(lang, 'nav.toggle')}
      >
        <svg
          width="24"
          height="24"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          stroke-linecap="round"
          aria-hidden="true"
        >
          <line x1="4" y1="7" x2="20" y2="7"></line>
          <line x1="4" y1="12" x2="20" y2="12"></line>
          <line x1="4" y1="17" x2="20" y2="17"></line>
        </svg>
      </button>

      <Button href={rdvHref} class="site-header__cta">
        {t(lang, 'nav.rdv')}
      </Button>
    </div>
  </header>

  {
    usedFallback && (
      <p class="fallback-notice container" role="note">
        {t(lang, 'fallback.notice')}
      </p>
    )
  }

  <main id="main">
    <slot />
  </main>

  <footer class="site-footer">
    <div class="container site-footer__grid">
      <div class="site-footer__brand">
        <p class="site-footer__name">{siteName}</p>
        <p>{t(lang, 'footer.tagline')}</p>
        {
          settings.social && (
            <ul class="site-footer__social">
              {settings.social.facebook && (
                <li>
                  <a
                    href={settings.social.facebook}
                    target="_blank"
                    rel="noopener noreferrer"
                    aria-label="Facebook"
                  >
                    <svg
                      width="20"
                      height="20"
                      viewBox="0 0 24 24"
                      fill="currentColor"
                      aria-hidden="true"
                    >
                      <path d="M13.5 21v-7h2.4l.4-3h-2.8V9.1c0-.9.3-1.5 1.6-1.5H16.4V4.9c-.3 0-1.2-.1-2.3-.1-2.3 0-3.9 1.4-3.9 4v2.2H7.8v3h2.4v7h3.3Z" />
                    </svg>
                  </a>
                </li>
              )}
              {settings.social.linkedin && (
                <li>
                  <a
                    href={settings.social.linkedin}
                    target="_blank"
                    rel="noopener noreferrer"
                    aria-label="LinkedIn"
                  >
                    <svg
                      width="20"
                      height="20"
                      viewBox="0 0 24 24"
                      fill="currentColor"
                      aria-hidden="true"
                    >
                      <path d="M6.9 8.6H4v11.4h2.9V8.6ZM5.5 4.1a1.7 1.7 0 1 0 0 3.4 1.7 1.7 0 0 0 0-3.4ZM20 13.5c0-2.9-1.6-4.9-4-4.9a3.5 3.5 0 0 0-3.1 1.7V8.6H10V20h2.9v-5.9c0-1.4.8-2.2 2-2.2 1.2 0 1.9.8 1.9 2.2V20H20v-6.5Z" />
                    </svg>
                  </a>
                </li>
              )}
              {settings.social.instagram && (
                <li>
                  <a
                    href={settings.social.instagram}
                    target="_blank"
                    rel="noopener noreferrer"
                    aria-label="Instagram"
                  >
                    <svg
                      width="20"
                      height="20"
                      viewBox="0 0 24 24"
                      fill="none"
                      stroke="currentColor"
                      stroke-width="1.8"
                      aria-hidden="true"
                    >
                      <rect x="3.5" y="3.5" width="17" height="17" rx="4.5" />
                      <circle cx="12" cy="12" r="4" />
                      <circle cx="17.2" cy="6.8" r="1.1" fill="currentColor" stroke="none" />
                    </svg>
                  </a>
                </li>
              )}
            </ul>
          )
        }
      </div>

      <div class="site-footer__newsletter">
        <NewsletterForm lang={lang} />
      </div>

      <nav aria-label={t(lang, 'footer.nav')}>
        <h2 class="visually-hidden">{t(lang, 'footer.nav')}</h2>
        <ul class="site-footer__links">
          {
            nav.map((item) => (
              <li>
                <a href={item.href}>{item.label}</a>
              </li>
            ))
          }
        </ul>
      </nav>

      <div class="site-footer__contact">
        <h2 class="site-footer__title">{t(lang, 'footer.contact')}</h2>
        <address class="site-footer__address">
          <p>{address.street}</p>
          <p>
            {address.zip}
            {address.city}, {address.country}
          </p>
          <p>
            <a href={telHref(settings.phone)}>{settings.phone}</a>
          </p>
          <p>
            <a href={`mailto:${settings.email}`}>{settings.email}</a>
          </p>
        </address>
      </div>
    </div>

    <div class="container site-footer__legal">
      <p>
        © {year}
        {siteName}. {t(lang, 'footer.rights')}
      </p>
      <nav aria-label={t(lang, 'footer.legal')}>
        <ul class="site-footer__legal-links">
          <li>
            <a href={`/${lang}/mentions-legales/`}>{t(lang, 'footer.mentions')}</a>
          </li>
          <li>
            <a href={`/${lang}/politique-de-confidentialite/`}>{t(lang, 'footer.privacy')}</a>
          </li>
        </ul>
      </nav>
    </div>
  </footer>
</BaseLayout>

<script>
  // Menu hamburger accessible : aria-expanded, fermeture par Échap et au
  // clic sur un lien. Script bundlé (compatible CSP script-src 'self').
  const toggle = document.querySelector<HTMLButtonElement>('.site-header__toggle');
  const nav = document.querySelector<HTMLElement>('#site-nav');

  if (toggle && nav) {
    const close = () => {
      nav.classList.remove('is-open');
      toggle.setAttribute('aria-expanded', 'false');
    };

    toggle.addEventListener('click', () => {
      const open = nav.classList.toggle('is-open');
      toggle.setAttribute('aria-expanded', String(open));
    });

    nav.querySelectorAll<HTMLAnchorElement>('a').forEach((link) => {
      link.addEventListener('click', close);
    });

    document.addEventListener('keydown', (event) => {
      if (event.key === 'Escape') close();
    });
  }
</script>

<style>
  .site-header {
    position: sticky;
    top: 0;
    z-index: var(--z-header);
    background-color: color-mix(in srgb, var(--color-bg) 88%, transparent);
    backdrop-filter: blur(8px);
    border-bottom: 1px solid var(--color-border);
  }

  .site-header__inner {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: var(--space-5);
    min-height: 4.5rem;
    padding-block: var(--space-3);
  }

  .site-header__brand {
    display: inline-flex;
    align-items: center;
    gap: var(--space-3);
    text-decoration: none;
    color: var(--color-ink);
    font-weight: 700;
  }

  .site-header__mark {
    width: 2.5rem;
    height: 2.5rem;
    flex: 0 0 auto;
  }

  .site-header__nav {
    margin-inline-start: auto;
  }

  /* Bouton hamburger : masqué sur desktop, visible <= 60rem. */
  .site-header__toggle {
    display: none;
    align-items: center;
    justify-content: center;
    width: var(--target-min);
    height: var(--target-min);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    background-color: var(--color-surface);
    color: var(--color-ink);
    cursor: pointer;
  }

  .site-header__toggle:hover {
    border-color: var(--color-primary);
    color: var(--color-primary);
  }

  .site-header__nav-list {
    display: flex;
    gap: var(--space-4);
    list-style: none;
    padding: 0;
    flex-wrap: wrap;
  }

  .site-header__nav-link {
    display: inline-flex;
    align-items: center;
    min-height: var(--target-min);
    padding-inline: var(--space-2);
    text-decoration: none;
    color: var(--color-ink);
    font-weight: 500;
  }

  .site-header__nav-link:hover {
    color: var(--color-primary);
  }

  .site-header__nav-link[aria-current='page'] {
    color: var(--color-primary);
    box-shadow: inset 0 -2px 0 var(--color-primary);
  }

  .fallback-notice {
    background: var(--color-accent-soft);
    color: var(--color-ink);
    padding: var(--space-3) var(--space-4);
    border-radius: var(--radius-md);
    margin-block: var(--space-4);
    font-weight: 600;
  }

  .site-footer {
    margin-top: var(--section-y);
    background-color: var(--color-bg-soft);
    border-top: 1px solid var(--color-border);
  }

  .site-footer__grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(14rem, 1fr));
    gap: var(--space-8);
    padding-block: var(--space-10);
  }

  .site-footer__name {
    font-weight: 700;
    font-size: var(--text-h4);
  }

  .site-footer__social {
    list-style: none;
    padding: 0;
    margin-top: var(--space-4);
    display: flex;
    gap: var(--space-3);
  }

  .site-footer__social a {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: var(--target-min);
    height: var(--target-min);
    border-radius: 50%;
    color: var(--color-ink-soft);
    background-color: var(--color-surface);
    border: 1px solid var(--color-border);
    transition:
      color var(--dur) var(--ease-out),
      border-color var(--dur) var(--ease-out);
  }

  .site-footer__social a:hover {
    color: var(--color-primary);
    border-color: var(--color-primary);
  }

  .site-footer__links,
  .site-footer__legal-links {
    list-style: none;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: var(--space-1);
  }

  .site-footer__links a,
  .site-footer__legal-links a {
    display: inline-flex;
    align-items: center;
    min-height: 2rem; /* cible ≥ 24 px (WCAG 2.2 — 2.5.8) */
  }

  .site-footer__contact {
    font-style: normal;
  }

  .site-footer__title {
    font-size: var(--text-h4);
    margin-bottom: var(--space-3);
  }

  .site-footer__legal {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    gap: var(--space-4);
    padding-block: var(--space-5);
    border-top: 1px solid var(--color-border);
    color: var(--color-ink-soft);
    font-size: var(--text-small);
  }

  .site-footer__legal-links {
    flex-direction: row;
    gap: var(--space-5);
  }

  /* <= 60rem : menu hamburger — la navigation devient un panneau
     masqué par défaut, ouvert par le bouton (aria-expanded). */
  @media (max-width: 60rem) {
    .site-header__toggle {
      display: inline-flex;
    }

    .site-header__nav {
      order: 5;
      flex-basis: 100%;
      display: none;
      margin-inline-start: 0;
      border-top: 1px solid var(--color-border);
      padding-block-start: var(--space-3);
    }

    .site-header__nav.is-open {
      display: block;
    }

    .site-header__nav-list {
      flex-direction: column;
      gap: var(--space-1);
    }

    .site-header__nav-link {
      width: 100%;
    }
  }

  /* Écrans très étroits : en-tête compact, sans débordement.
     Le CTA « Prendre RDV » reste accessible (hero + footer). */
  @media (max-width: 30rem) {
    .site-header__inner {
      gap: var(--space-3);
      padding-block: var(--space-2);
    }

    .site-header__cta {
      display: none;
    }

    .site-header__brand {
      gap: var(--space-2);
    }

    .site-header__mark {
      width: 2rem;
      height: 2rem;
    }

    .site-header__nav-list {
      gap: var(--space-2);
    }

    .site-header__nav-link {
      padding-inline: var(--space-1);
      font-size: var(--text-small);
    }
  }
</style>
