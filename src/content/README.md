# Contenus CMS (incrément 3)

Ce dossier recevra les collections de contenu éditées via Decap CMS
(`admin/`) : `settings`, `pages`, `testimonials`, `faq`, `blog`.

Le modèle (schémas Zod) sera déclaré dans `src/content.config.ts` à
l'incrément 3, conformément au modèle validé à l'étape 0.
